.container-signup
  .right-top.button--gray
    = link_to "登録", new_registration_path(resource_name)
  .form
    .logo-area
      = link_to image_tag("logo.png", class: "logo", size: "44x44", alt: "logo"), "/"
    .greeting-area
      %h1.greeting 他のピンを見るにはログインしてください
    .form-area
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        = f.email_field :email, placeholder: "メール", class: "field-input"
        = f.password_field :password, autocomplete: "off", placeholder: "パスワード", class: "field-input"
        = f.submit "Log in", class: "button--red wide"
      .notice パスワードをお忘れの場合は再度アカウントを作成してください。
  .footer
    %ul.footer__list
      %li.footer__list__item Pincredible について
      %li.footer__list__item 利用規約
      %li.footer__list__item ヘルプ
